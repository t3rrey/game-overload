import React from 'react';
import { Route, Redirect } from 'react-router-dom';

const PublicRoute = () => {
    return (
        <div></div>
        /* Later on check if signed in and if they are than render
        the component but if not then redirect to sign in page */
    );
};

export default PublicRoute;